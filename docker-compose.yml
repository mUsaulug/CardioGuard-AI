version: "3.8"
services:
  cardioguard:
    build: .
    container_name: cardioguard-ai
    ports:
      - "8000:8000"
    environment:
      - CORS_ORIGINS=*
    volumes:
      - ./reports:/app/reports
    restart: unless-stopped
